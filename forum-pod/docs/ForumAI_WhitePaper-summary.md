# ForumAI White Paper — implementation summary (interim)

Until `ForumAI_WhitePaper.docx` is converted to markdown, engineering uses **Articles Art II** as the product requirements of record:

1. **Sovereign non-custodial** civic feedback network.
2. Member **retains ownership**; Association is **fiduciary** only.
3. **Aggregated** community intelligence and optional marketplace (later milestone).

## Build `1.3-solid-webauthn`

- Personal Pod on community CSS (Solid + WebAuthn device gate).
- Local SQL over DuckDB (truth UI on device).
- Cooperative path: **opt-in signed export** only.
